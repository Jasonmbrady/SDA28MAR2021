class Queue {
    constructor() {
      // 0 is the front
      // arr.length-1 is the back
      this.items = []
    }
  
    enqueue (val) {
    // Add a value to the back of Queue and return new length
    }
  
    dequeue () {
      // Remove and return value from front
    }
  
    isEmpty () {
      // return T if empty, F if not
    }
  
    size () {
      // return number of items in the Queue
    }
  
    front () {
      // Like Peek, returns the value of the item in the front without removing it from the queue.xs
    }
  
  }